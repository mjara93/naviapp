<!-- error message -->
<% if notice != nil %>
<div class="alert alert-success" role="alert">
    <button type="button" class="close" data-dismiss="alert">
        <span aria-hidden="true">&times;</span>
        <span class="sr-only">Close</span>
    </button>
    <%= notice %>
</div>
<% end %>
<!-- end error message -->
<!-- body -->

<div class="row">
    <div class="col-md-4">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <h3 class="panel-title">Datos</h3>
            </div>
            <div class="panel-body">
              <p>
                <strong>Rut:</strong>
                <%= @provider.rut %>
              </p>

              <p>
                <strong>Nombre:</strong>
                <%= @provider.nombre %>
              </p>

              <p>
                <strong>Direccion:</strong>
                <%= @provider.direccion %>
              </p>

              <p>
                <strong>Numero:</strong>
                <%= @provider.numero %>
              </p>

              <p>
                <strong>Ciudad:</strong>
                <%= @provider.city.nombre %>
              </p>

              <p>
                <strong>Email:</strong>
                <%= @provider.email %>
              </p>

              <p>
                <strong>Telefono:</strong>
                <%= @provider.telefono %>
              </p>
            </div>
            <%= form_for(@provider) do |provider| %>
            <div class="panel-footer">
                <%= link_to 'Editar', edit_provider_path(@provider), class:"btn btn-default pull-left" %>
                <button type="button" class="btn btn-primary pull-right mb-control" data-box="#message-box-warning">Eliminar</button>
                <%= link_to 'Volver', providers_path, class:"btn btn-default pull-left" %>
            </div>
        </div>
    </div>
    <div class="col-md-8">
      <div class="table-responsive">
          <table class="table datatable">
              <thead>
                  <tr>
                      <th>Factura</th>
                      <th>Total</th>
                      <th>Fecha</th>
                  </tr>
              </thead>

              <tbody>
                  <% @provider.purchases.each do |find| %>
                  <tr>
                      <td><%= find.factura %></td>
                      <td><%= find.total %></td>
                      <td><%= find.fecha %></td>
                  </tr>
                  <% end %>
              </tbody>
          </table>
      </div>

    </div>
</div>
<!-- end body -->
<!-- danger -->
<!-- warning -->
        <div class="message-box message-box-warning animated fadeIn" id="message-box-warning">
            <div class="mb-container">
                <div class="mb-middle">
                    <div class="mb-title"><span class="fa fa-warning"></span> Atención</div>
                    <div class="mb-content">
                      <p>¿Está seguro de eliminar el registro? todas las compras realizadas a este proveedor serán eliminadas.</p>
                    </div>
                    <div class="mb-footer">
                        <button class="btn btn-default btn-lg pull-left mb-control-close">Cerrar</button>
                        <button class="btn btn-default btn-lg pull-right mb-control mb-control-close" data-box="#message-box-danger">Continuar</button>
                    </div>
                </div>
            </div>
        </div>
<!-- end warning -->
<!-- danger -->
<div class="message-box message-box-danger animated fadeIn" id="message-box-danger">
    <div class="mb-container">
        <div class="mb-middle">
            <div class="mb-title">
                <span class="fa fa-times"></span>
                Peligro</div>
            <div class="mb-content">
                <p>¿Está seguro de continuar con la eliminación del registro? todos los datos serán borrados de forma permanente.</p>
            </div>
            <div class="mb-footer">
                <%= link_to 'Eliminar', @provider, method: :delete, class:"btn btn-default btn-lg pull-right"%>
                <button class="btn btn-default btn-lg pull-left mb-control-close">Cerrar</button>
            </div>
        </div>
    </div>
</div>
<!-- end danger -->
<% end %>
